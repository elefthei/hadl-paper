%%%%%%%%%%%%% Packages
\usepackage[utf8]{inputenc}
\usepackage[dvipsnames]{xcolor}
\usepackage{mathpartir}
\usepackage{tikz}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{subcaption}
\graphicspath{{figures/}}
\usepackage{changepage}
\usepackage{etoolbox}
\usepackage{booktabs}
\usepackage{listings}
\usepackage{float}
\usepackage{xspace}
\usepackage[true]{anonymous-acm} % For anonymous submission; change to 'false' for camera-ready
\usetikzlibrary{shapes.geometric, shapes.multipart, arrows.meta, positioning, calc, backgrounds, patterns, decorations.pathreplacing}

\hfuzz=70pt

%%%%%%%%%%%%%% Comments
\usepackage[]{comments} % add option 'hide' to hide all
\Commenter{lef}

%%%%%%%%%%%%% Listings setup for Pact code examples
% Three keyword classes:
%   [1] top-level constructs (workflow)         — black bold
%   [2] expression-level keywords               — dark green bold
%   [3] types / built-in schema names           — dark red bold
\lstdefinelanguage{pact}{
  sensitive=true,
  alsoletter={0123456789},
  % Top-level (class 1): black bold
  morekeywords=[1]{workflow},
  keywordstyle=[1]\bfseries\color{black},
  % Expressions (class 2): dark green bold
  morekeywords=[2]{let, var, restrict, gen, agent, ask, say, if, else, while, for, in, type, enforce, eval, as, js},
  keywordstyle=[2]\bfseries\color{green!50!black},
  % Types (class 3): dark red bold
  morekeywords=[3]{root, Array, String, Number, Bool, Schema, Policy},
  keywordstyle=[3]\bfseries\color{red!50!black},
  comment=[l]{//},
  morestring=[b]",
  stringstyle=\itshape\color{gray},
  breaklines=true,
  deletestring=[b]",
  deletestring=[b]',
  moredelim=[s][\color{gray}]{"}{"},
  moredelim=*[is][\color{black!85}\ttfamily\itshape]{`}{`},
  xleftmargin=\parindent,
  showstringspaces=false,
  basicstyle=\small\ttfamily,
  commentstyle=\itshape\color{red!40!black},
  identifierstyle=\itshape,
  escapeinside={<@}{@>}
}
\lstset{
  basicstyle=\ttfamily\small,
  columns=flexible,
  keepspaces=true,
  showstringspaces=false,
  numbers=left,
  numberstyle=\tiny\color{gray},
  numbersep=5pt,
  xleftmargin=2em,
  frame=single,
  framesep=3pt,
  aboveskip=1em,
  belowskip=1em,
  escapeinside={(*@}{@*)},
}

%%%%%%%%%%%%% Reference macros
\newcommand{\sect}[1]{Section~\ref{#1}\xspace}
\newcommand{\figr}[1]{Figure~\ref{#1}\xspace}
\newcommand{\tabl}[1]{Table~\ref{#1}\xspace}
\newcommand{\defn}[1]{Definition~\ref{#1}\xspace}
\newtheorem{definition}{Definition}

%%%%%%%%%%%%% Custom terms
\newcommand{\sys}{Pact\xspace}
\newcommand{\pact}{\sys}
\newcommand{\TODO}[1]{\textbf{\textcolor{red}{[TODO:~#1]}}}

%%%%%%%%%%%%% Theorem / judgment environments
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\newtheorem{remark}{Remark}

%%%%%%%%%%%%% Pact formal model — machine components
% Four-tuple configuration ⟨Θ, Π, σ, e⟩: heal context, installed policy,
% mutable store, expression under reduction. \MConfig elides σ (used for
% rules that leave the store unchanged); \MConfigS shows all four fields
% (used for the mutable-state rules).
\newcommand{\MConfig}[3]{\langle #1,\; #2,\; #3 \rangle}
\newcommand{\MConfigS}[4]{\langle #1,\; #2,\; #3,\; #4 \rangle}
\newcommand{\MErr}{\Sigma}
\newcommand{\MPol}{\Pi}
\newcommand{\MStore}{\sigma}
\newcommand{\MPrin}{\pi}
\newcommand{\MRetry}{N}
\newcommand{\Subst}[2]{#1[#2/x]}

%%%%%%%%%%%%% Pact formal model — event log (heal context)
\newcommand{\ErrEv}[1]{\mathsf{err}(#1)}
\newcommand{\SucEv}{\mathsf{succ}}

%%%%%%%%%%%%% Pact formal model — types
\newcommand{\Tunit}{\texttt{()}}
\newcommand{\Tstring}{\texttt{String}}
\newcommand{\Tnum}{\texttt{Number}}
\newcommand{\Tbool}{\texttt{Bool}}
\newcommand{\Tschema}{\texttt{Schema}}
\newcommand{\Tpolicy}{\texttt{Policy}}
\newcommand{\Tprincipal}{\texttt{Principal}}
\newcommand{\Tpath}{\texttt{Path}}
\newcommand{\Tarrow}[2]{#1 \to #2}
\newcommand{\Trec}[1]{\{\,#1\,\}}
\newcommand{\Tarr}[1]{\texttt{array}[#1]}
\newcommand{\Tref}[1]{#1}
% Healable type predicate.
\newcommand{\Healable}[1]{\mathsf{healable}(#1)}
% Hard-error sink value produced by \rulename{Gen-TypeErr} and
% \rulename{Gen-Budget}; intentionally untypeable.
\newcommand{\errV}{\bot_{\mathsf{err}}}
\newcommand{\unit}{\texttt{()}}

%%%%%%%%%%%%% Pact formal model — expressions (named variables, substitution-based)
\newcommand{\Ask}[1]{\texttt{ask}\;#1}
\newcommand{\Say}[1]{\texttt{say}\;#1}
% gen: surface syntax is `gen s as π`; the type annotation lives on the
% enclosing `let x : τ = gen s as π`. We keep τ as a macro argument for
% machinery (every call site threads τ) but do not render it, to match
% fig:syntax.
\newcommand{\Gen}[3]{\texttt{gen}\;#2\;\texttt{as}\;#3}
% agent carries its principal in-syntax: \Agent{s}{π}
\newcommand{\Agent}[2]{\texttt{agent}\;#1\;\texttt{as}\;#2}
\newcommand{\Eval}[2]{\texttt{eval}\;#1\,(#2)}
\newcommand{\Clos}[2]{\lambda(#1).\,#2}
\newcommand{\Enforce}[1]{\texttt{enforce}\;#1}
\newcommand{\Js}[1]{\texttt{js}\;#1}
% Principal introducers. `\Eroot` is the universal principal; `\Erestrict{e}`
% creates a fresh strictly weaker principal from an existing one.
\newcommand{\Eroot}{\texttt{root}}
\newcommand{\Erestrict}[1]{\texttt{restrict}\;#1}
% Principal value: a path in the restriction hierarchy rooted at `root`. `p`
% ranges over Principal values; we elide the underlying tag-chain encoding.
\newcommand{\Vprincipal}[1]{\langle\!\langle #1 \rangle\!\rangle}
% Let: named binder `x`.
\newcommand{\Let}[3]{\texttt{let}\; x\,{:}\,#1 = #2\;\texttt{in}\;#3}
\newcommand{\Iff}[3]{\texttt{if}\;(#1)\;\{#2\}\;\texttt{else}\;\{#3\}}
\newcommand{\Whl}[2]{\texttt{while}\;(#1)\;\{#2\}}
% For: named binder `x`.
\newcommand{\Forr}[2]{\texttt{for}\;x\;\texttt{in}\;#1\;\{#2\}}
\newcommand{\Seq}[2]{#1\,;\,#2}

%%%%%%%%%%%%% Pact formal model — judgments
% StType: typing judgment for closed values (⊢ v : τ).
\newcommand{\StType}[2]{\vdash #1 : #2}
% Type-check judgment — single-valued, returns either ok or err(ε).
% First arg is the typing context (∅ for closed values, x:τ for
% continuations under a let-binder).
\newcommand{\TJok}[3]{#1 \vdash #2 : #3 \;\Rightarrow\; \mathsf{ok}}
\newcommand{\TJerr}[4]{#1 \vdash #2 : #3 \;\Rightarrow\; \mathsf{err}(#4)}
% Context for continuation checks: just the single let-bound variable.
\newcommand{\Gext}[2]{#1 {:} #2}
% PolOk removed: no static policy judgment. PolHeal uses π ∉ dom(Π).
\newcommand{\WF}[1]{\vdash #1 : \mathsf{wf}}

%%%%%%%%%%%%% Pact formal model — reduction and oracle
\newcommand{\step}{\longrightarrow}
\newcommand{\stepP}{\longrightarrow_{\mathsf{p}}}
\newcommand{\steps}{\longrightarrow^{*}}
\newcommand{\Extr}[1]{\mathtt{Extract}(#1)}
\newcommand{\Oracle}[3]{\mathcal{O}(#1,\,#2,\,#3)}
\newcommand{\JOracle}[2]{\mathcal{J}(#1,\,#2)}
\newcommand{\Explain}[2]{\mathsf{explain}(#1,\,#2)}
\newcommand{\Recheck}[2]{\mathsf{recheck}(#1,\,#2)}
\newcommand{\Prov}{\mathsf{prov}}
\newcommand{\Allow}[1]{\mathsf{allow}(#1)}
\newcommand{\Install}[2]{\mathsf{install}(#1,\,#2)}
% Static policy premise: principal must be declared in current policy state.
% Action argument retained for backwards compatibility with existing rule
% sites; it is intentionally ignored by the macro because the abstract
% semantics enforces only principal-membership statically. The action and
% resource of any specific access are checked at runtime by the policy
% monitor (see Section~\ref{sec:implementation}).
\newcommand{\PrinDeclared}[2]{#2 \in \mathrm{dom}(#1)}
\newcommand{\PolAllow}[3]{\PrinDeclared{#1}{#2}}
% Install discipline at \enforce: every rule in the candidate policy is
% forbid-effect, and no forbid rule targets root.
\newcommand{\InstallOk}[1]{\mathsf{forbidOnly}(#1) \wedge \mathsf{rootSafe}(#1)}

%%%%%%%%%%%%% Pact formal model — rule-name shortcut
\newcommand{\rulename}[1]{\textsc{#1}}

